import 'package:flutter/material.dart';

/// Stub device history page — placeholder until full implementation.
class DeviceHistoryPage extends StatelessWidget {
  const DeviceHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Device History')),
      body: const Center(child: Text('Coming soon')),
    );
  }
}
